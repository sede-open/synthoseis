/** Central configuration for the Synthoseis webapp.
 *
 * MANIFEST_URL: path/URL to the manifest.json file.
 * Zarr files are served via the /api/zarr/{path} endpoint — no base URL needed.
 */

export const MANIFEST_URL = "/api/manifest";
